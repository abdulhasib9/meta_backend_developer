from pathlib import Path
from zipfile import ZipFile

# with ZipFile("test.zip","w") as zip:
#     for path in Path("builtinlibrary/").rglob("*.*"):
#         zip.write(path)

# with ZipFile("test.zip") as zip:
#     print(zip.namelist())